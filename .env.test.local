# Test Environment Configuration
# Note: Using the same database as development. Tests should clean up their data.
# Test data should use identifiable prefixes like "TEST_" to distinguish from real data.
#####DATABASE_URL='postgresql://neondb_owner:npg_gGMhedJ8fN2r@ep-muddy-firefly-a1bmcs82-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require'

# Test Server Configuration
NODE_ENV=test
BASE_URL=http://localhost:3000

# JWT Secret (same as dev for compatibility)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Test-specific settings
SKIP_ENV_VALIDATION=true
